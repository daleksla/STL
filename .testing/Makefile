# standard compile options for the c++ executable
STD = --std=c++17

# deposit directories
SOURCES = ./sources
EXES = ./exes

# default super-target
all: 
	g++ $(STD) $(SOURCES)/stackTest.cpp -o $(EXES)/stackTest
	g++ $(STD) $(SOURCES)/queueTest.cpp -o $(EXES)/queueTest
